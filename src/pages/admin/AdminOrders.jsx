import { useApp } from "../../context/AppContext";
const next = { Preparing: "Ready", Ready: "Completed", Completed: null };
const statusStyles = { Preparing: "bg-warning/15 text-warning", Ready: "bg-success/15 text-success", Completed: "bg-muted text-muted-foreground" };
export default function AdminOrders() {
  const { orders, updateOrderStatus, updatePaymentStatus } = useApp();
  return (<main className="p-4 sm:p-6"><h1 className="text-2xl font-bold">Order Management</h1><p className="text-sm text-muted-foreground">Verify the customer's token, confirm payment, and move orders through their lifecycle.</p>{orders.length === 0 ? <p className="mt-10 rounded-2xl border border-dashed border-border p-10 text-center text-sm text-muted-foreground">No orders yet — place one from the user side to see it here.</p> : <div className="mt-5 grid gap-3 lg:grid-cols-2">{orders.map((o) => { const advance = next[o.status]; return <div key={o.id} className="rounded-2xl border border-border bg-card p-4" style={{ boxShadow: "var(--shadow-card)" }}><div className="flex items-start justify-between gap-3"><div><p className="text-xs text-muted-foreground">Token</p><p className="text-3xl font-black text-primary">{o.token}</p>{o.username && <p className="text-[11px] text-muted-foreground">by @{o.username}</p>}</div><div className="flex flex-col items-end gap-1"><span className={`rounded-full px-3 py-1 text-xs font-semibold ${statusStyles[o.status]}`}>{o.status}</span><button onClick={() => updatePaymentStatus(o.id, o.payment === "Paid" ? "Pending" : "Paid")} className={`rounded-full px-2 py-0.5 text-[10px] font-semibold transition ${o.payment === "Paid" ? "bg-success/15 text-success hover:bg-success/25" : "bg-warning/15 text-warning hover:bg-warning/25"}`} title="Click to toggle payment status">{o.payment}</button></div></div><p className="mt-1 text-xs text-muted-foreground">Table T{o.table} • ₹{o.total}</p><ul className="mt-3 space-y-1 text-sm">{o.items.map(({ item, quantity }) => <li key={item.id} className="flex justify-between"><span>{item.name} <span className="text-muted-foreground">× {quantity}</span></span><span>₹{item.price * quantity}</span></li>)}</ul>{advance && <button onClick={() => updateOrderStatus(o.id, advance)} className="mt-4 w-full rounded-xl py-2 text-sm font-bold text-primary-foreground" style={{ background: "var(--gradient-primary)" }}>Mark as {advance}</button>}</div>; })}</div>}</main>);
}
